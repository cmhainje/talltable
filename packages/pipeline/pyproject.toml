[project]
name = "talltable-pipeline"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "Connor Hainje", email = "cmhainje@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "healpy (>=1.18.1,<2.0.0)",
    "numpy (>=2.3.3,<3.0.0)",
    "pyarrow (>=21.0.0,<22.0.0)",
    "astropy (>=7.1.0,<8.0.0)",
    "duckdb (>=1.4.1,<2.0.0)",
    "astroquery (>=0.4.10,<0.5.0)",
    "scipy (>=1.16.2,<2.0.0)",
    "tqdm (>=4.67.1,<5.0.0)",
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
talltable = { workspace = true }
